<!DOCTYPE html>
<html lang="en">
<head>
    <title>Music Store Invetory</title>
</head>
<body>
    <h1>Music Store Invetory</h1>

    <h2>Categories</h2>

    <ul>
        <% categories.forEach(category => { %>
            <li>
                <a href="/categories/<%= category.id %>">
                    <%= category.name %>
                </a>
            </li>
        <% }) %>
    </ul>
</body>
</html>